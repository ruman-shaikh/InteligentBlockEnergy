**Run the pplication and the API**
Make sure to activate the virtual enviroment
```
python -m backend.app
```

**Run a peer instance**
Make sure to activate the virtual enviroment
```
set PEER=True
python -m backend.app
```

**To test the pubsub layer**
Make sure to activate the virtual enviroment
```
python -m backend.pubsub
```